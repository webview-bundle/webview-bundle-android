<?xml version="1.0" encoding="UTF-8"?>
<lint>
    <!--
      wvb_ffi.kt is generated by uniffi and installed from the upstream release
      (see scripts/install.mjs). Its java.lang.ref.Cleaner usage (API 33) is
      selected at runtime behind a Build.VERSION.SDK_INT check that lint cannot
      see across, so NewApi is a false positive there. Scoped to that file only,
      by path, so the check stays enforced for hand-written code and survives FFI
      regeneration.
    -->
    <issue id="NewApi">
        <ignore path="src/main/kotlin/dev/wvb/wvb_ffi.kt" />
    </issue>
</lint>
